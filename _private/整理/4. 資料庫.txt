資料庫必須符合 ACID 特性，其中 Isolation (隔離性) 最難維護，容易產生併發異常。
為了確保隔離性，資料庫採用了併發控制機制。
最常見的機制就是基於悲觀鎖 (Pessimistic Locking) 的 S-Lock / X-Lock。
為了讓這些鎖完美運作、不出錯，交易必須嚴格遵守 2PL (二階段鎖定協議)。
併發控制機制，
二階段鎖定協定（two-phase locking protocol）可以保證有衝突的交易在更新資料時仍保持資料的正確性。請說明什麼是二階段鎖定協定，並解釋為什麼二階段鎖定能使有衝突的交易仍能被正確的執行。









---------------------------------------------------------------------------
變數
-- 相當於 JS 的: let a = (SELECT SUM(amount) FROM expenses);
SELECT SUM(amount) INTO @a FROM expenses;

-- 把變數拿出來看，或是做後續運算
SELECT @a; 
SELECT @a * 1.05; -- 例如加上 5% 稅金

---------------------------------------------------
DECLARE a INT;  -- 先宣告一個整數變數 a (類似 let a;)
SELECT SUM(amount) INTO a FROM expenses; -- 把結果塞進去





---------------------------------------------------------------------------