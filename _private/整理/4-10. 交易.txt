資料庫資料更新流程
1. 發送 UPDATE 命令與「搜尋核對」
2. 在記憶體中修改資料
3. 發送 COMMIT 命令給資料庫，宣告交易準備結束。
4. 寫入交易記錄檔（Transaction Log），該步驟完成的這一瞬間，交易結束。
5. 背景非同步寫回硬碟
---------------------------------------------------------------------------
交易（Transaction）是資料庫中一個不可分割的工作單位，由一連串操作組成。核心精神為 全部成功 或 全部失敗。
ACID四大特性：
  1. 原子性，交易中所有操作"全部完成"或"全部不做"；失敗時以 ROLLBACK 復原。
  2. 隔離性，多個交易並行時互不干擾，一交易的中間結果不被其他交易看見；以鎖定（Locking）或 MVCC 達成。
    負責處理「時間順序與併發排隊」，把「同時發生」的混亂交易，強制排成一條有秩序的單行道。
  3. 持久性，交易一旦 COMMIT，變更即永久保存，即使系統故障也不遺失；以交易記錄檔（log）達成。
  4. 一致性，交易執行前後，資料庫都維持在符合所有完整性限制的合法狀態。(此為終極目標)
  ***原子性 (A) + 隔離性 (I) + 持久性 (D) = 一致性 (C)***

隔離性延伸：
1. 併發控制機制 (Concurrency Control)：
  1. 鎖定（Locking）(悲觀鎖)：資料庫用來控制多個使用者同時存取同一筆資料的基礎防護機制總稱。(傳統的「公廁排隊」模式，缺點是，排隊，效率差。)
     一種「先上鎖再做事」的策略，假設衝突一定會發生，所以讀取時就立刻獨佔資源（如 FOR UPDATE），適用於高衝突場景。
  2. 樂觀鎖：不是鎖，屬於一種「不鎖門，做事後才檢查」的策略，假設衝突很少發生，直到更新（UPDATE）時才核對資料有沒有被別人改過，適用於低衝突場景。
     缺點：因為是最後一步才檢查，所以如果海量資料，伺服器會一瞬間收到一堆失敗的UPDATE，且會有大量使用者，會在最後一步交易失敗；使用者體驗不好。
2. MVCC（多版本併發控制）是一種透過『保留資料歷史版本快照』來達成『讀寫互不阻塞』的底層機制。
    它讓讀取操作不需要等待寫入鎖定，大幅提升了資料庫在高併發環境下的效能。(如果不加FOR UPDATE會自動套用)
3. 交易記錄檔：用來記錄所有已提交（COMMIT）但尚未寫入資料檔的變更，以確保系統崩潰時能還原資料（實現持久性 Durability）。」 (SQL內部自行處理)
MVCC（多版本併發控制）：(SQL內部自行處理)


ex. 悲觀鎖、樂觀鎖範例
有一張 accounts（帳戶）資料表。
你要提款 500 元，目前資料庫裡的狀態是：
id = 1（你的帳戶）
balance = 1000（餘額 1000 元）
version = 1（資料版本號，這是給樂觀鎖用的，預設是 1）

-- 悲觀鎖
-- 1. 開啟交易防護罩
START TRANSACTION;

-- 2. 查詢餘額，並【強制上鎖】 (此時別人完全不能動這筆資料，只能排隊乾等)
SELECT balance 
FROM accounts 
WHERE id = 1 
FOR UPDATE;

---------------------------------------------------
-- [後端程式邏輯]
-- 程式拿到 balance = 1000
-- 判斷 1000 >= 500 (餘額充足，允許提款)
---------------------------------------------------

-- 3. 執行扣款
UPDATE accounts 
SET balance = balance - 500 
WHERE id = 1;

-- 4. 提交交易，並【自動解鎖】
COMMIT;


-- 樂觀鎖：是一種「不用鎖的應對策略」其實不是鎖
-- 1. 查詢餘額與【當前版本號】 (一般 SELECT，不加鎖，大家都可以同時查)
SELECT balance, version 
FROM accounts 
WHERE id = 1;

---------------------------------------------------
-- [後端程式邏輯]
-- 程式拿到 balance = 1000, version = 1
-- 判斷 1000 >= 500 (餘額充足，準備提款)
---------------------------------------------------

-- 2. 執行扣款，但把剛剛拿到的 version = 1 當作【核對條件】
-- 同時將 version 升級 (+1)
UPDATE accounts 
SET balance = balance - 500,
    version = version + 1
WHERE id = 1 
  AND version = 1;  -- 👈 關鍵防線：必須還是第 1 版，我才要更新！

---------------------------------------------------
-- [後端程式邏輯 - 驗證結果]
-- 資料庫會回傳「影響了幾筆資料 (Affected rows)」：
-- 
-- 狀況 A：回傳 1 筆 -> 代表沒人動過，提款成功！(自動 COMMIT)
-- 狀況 B：回傳 0 筆 -> 代表在你操作的瞬間，別人已經改過這筆資料 (version 已經不是 1 了)。
--                   這時後端程式就要拋出錯誤訊息給使用者：「資料已變更，請重新整理」。
---------------------------------------------------
交易控制指令（TCL）
-- 🚪 開啟交易防護罩
START TRANSACTION;
UPDATE users SET balance = balance - 10000 WHERE id = 1;

-- 💾 ★ 設立存檔點 (SAVEPOINT)，在此之前的狀態、記錄存起來
SAVEPOINT point_flight;

INSERT INTO orders (user_id, item, status) VALUES (1, '機票', '成功');
-- ⏪ ★ 回復到指定存檔點 (部分 ROLLBACK)
ROLLBACK TO SAVEPOINT point_flight;

-- ROLLBACK，把全部的操作徹底撤銷 (ROLLBACK對應 交易的原子性：全有或全無)
ROLLBACK;
-- ✅ ★ 永久寫入 (COMMIT，對應持久性)
COMMIT;
---
隔離性是確保多筆並發交易互不干擾，使其宛如獨立執行以維持資料一致性的核心機制。
隔離等級則是資料庫提供的設定規範，讓開發者根據業務需求，在「資料嚴謹度」與「系統併發效能」之間進行權衡與取捨。
隔離等級，幾乎都是為了決定 SELECT 該如何表現而設計的。至於WRITE，所有等級的防護底線都一樣嚴格。
---
三種讀取異常：
髒讀（Dirty Read）：讀到另一交易尚未提交的資料。
不可重複讀（Non-repeatable Read）：在同一交易內前後讀取同一筆資料，卻因其他交易在期間內完成「更新 (Update) 或刪除 (Delete)」，導致兩次讀取的數值或狀態不一致。
幻讀（Phantom Read）：在同一交易內前後執行相同的範圍查詢，卻因其他交易在期間內「新增 (Insert)」或 「刪除 (Delete)」了符合條件的紀錄，導致第二次查詢得到的資與之前不相同。
---
隔離等級：
Read Uncommitted	讀未提交，完全無防禦力
Read Committed	讀已提交
Repeatable Read	可重複讀，確保交易內針對同一筆紀錄的多次讀取結果恆定 (MYSQL預設)
Serializable	序列化 / 串行化，最高隔離層級，強制所有並發交易必須循序排隊執行。

** Serializable 最嚴格，但效能極差，但能防止全部讀取異常。
** 讀取異常的難以預防的程度： 幻讀(僅靠序列化才能防) > 不可重複讀 > 髒讀

** Serializable 屬於 「隔離等級 (Isolation Levels)」 的範疇，探討的是系統整體的狀態。
** FOR UPDATE 屬於 「鎖定機制 (Locking Mechanisms) / 悲觀鎖」 的範疇，探討的是開發者如何手動控制併發。




























