以下是針對各路由所需要的內容，另外，需要新增路由
我把每個路由需要的txt都放入 @/_private/
-----
計概: 計算機概論.txt
網概: 網概.txt
資料庫: 資料庫.txt + 資料結構與演算法.txt
資管: 資訊管理.txt
程式:  程式.txt + 系統分析與設計.txt

演算法:  資料結構與演算法.txt 當中的 排序 的範例要在這邊實作  + 國考常見演算法_Java遞迴非遞迴_時間複雜度.md

每個txt你可以先閱讀，數量非常多。

以上是針對各路由需要參考的內容。
每個txt都有考試的大鋼、以及需要記憶、了解的內容。 但目前僅有標題、一些些必考重點，詳細的內容需要AI進行補充

-----

演算法的部分，需列出 資料結構與演算法.txt 內容有提到的"排序"，及部分常見演算法
Bubble Sort | O(n) | O(n^2) | O(n^2) | 穩定 | 若有 early stop 最好 O(n)
Selection Sort | O(n^2) | O(n^2) | O(n^2) | 通常不穩定 | 交換次數少
Insertion Sort | O(n) | O(n^2) | O(n^2) | 穩定 | 適合小資料或近乎排序
Merge Sort | O(n log n) | O(n log n) | O(n log n) | 穩定 | 需要額外空間
Quick Sort | O(n log n) | O(n log n) | O(n^2) | 不穩定 | pivot 選不好會退化
Heap Sort | O(n log n) | O(n log n) | O(n log n) | 不穩定 | 原地排序，利用 heap
Shell Sort | 視 gap 而定 | 視 gap 而定 | 可到 O(n^2) | 不穩定 | 插入排序改良 |

國考常見演算法_Java遞迴非遞迴_時間複雜度.md 則是我已經有寫好一些常見的演算法，也需要收錄
每個排序法 要做 遞迴、非遞迴版本，以及要算出 時間複雜度。

-----

如果是程式碼的範例，要提供註解，要讓考官知道程式碼的思路、想法、用意


除了 與 英文 與國文，其他的風格應該一致。




任務數量無上限。


由於是國考要用，禁止任何錯誤。
本專案讀者請以 讀者為新手的 角度來生成內容，本專案當作自學的學習文件，所以不能有錯誤，且註解清晰、明白、手把手教學，用語要通俗。


需要運用大量副代理製作，每個txt提供的 有大量的 主題，大致流程為以下:
1. 每一個主題一個副代理去執行，生成本專案要用的 學習用的內容，假設先開啟副代理A，然後在 @/_TMP/  放置副代理生成的內容，檔案名稱用時間序列，方便記憶與追蹤。
2. 每次副代理處理完一個 主題，關閉該副代理，避免後續汙染。 所以 副代理A關閉。
3. 之後再開啟一個副代理C，專門去驗證 剛剛副代理A生成的內容，發現問題 就把它優化；直接在剛剛副代理A生成的內容進行調整。
4. 關閉副代理C
5. 主代理 或 副代理D，在對剛剛副代理C調整過的內容寫入本專案。 (這部分你先幫我分析，如何才適合)

