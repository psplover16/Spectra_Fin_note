## 1. 測試基線

- [x] [P] 1.1 為 Networking v2 route uses filename-backed topics 與 Use source filenames as the order manifest and sanitized topic titles 建立失敗測試：驗證 `/networking-v2` route-visible topic ids、12 篇 sourceFiles、清理後 titles 與檔名章節順序完全符合 spec 表格；以 `npx vitest run tests/unit/networkingV2RouteWorkflow.spec.ts tests/unit/subjectTopics.spec.ts` 確認實作前失敗。
- [x] [P] 1.2 為 Add networkingV2 as an independent route and subject key、Networking v2 subject route、Networking v2 route has independent topic ownership、Keep progress storage on the existing v1 localStorage shape 建立失敗測試：驗證 `SubjectKey`、`PrimaryRoutePath`、router、preload、placeholder、progress empty state/normalization 都包含 `networkingV2`，且 v2 topic ids 不出現在 `networking`、`computerPrinciples`、`computerPrinciplesV2`、`digitalLogic`、`operatingSystems`；以 `npx vitest run tests/unit/routeConfig.spec.ts tests/unit/routePreload.spec.ts tests/unit/subjectTopicProgressStorage.spec.ts tests/unit/networkingV2RouteWorkflow.spec.ts` 確認實作前失敗。
- [x] [P] 1.3 為 Computer foundation menu exposes Networking v2 與 Keep the entry inside the computer foundation menu 建立失敗測試：驗證計概類選單列出 `網路概論(v2)`、導向 `/networking-v2`、root redirect 仍是 `/computer-principles`、header 不新增 top-level `網路概論(v2)` tab；以 `npx vitest run tests/component/ComputerFoundationSubjectSwitcher.spec.ts tests/component/SubjectRoutesSmoke.spec.ts tests/component/AppShellSmoke.spec.ts` 與 `npx playwright test tests/e2e/app-shell.smoke.spec.ts -g "computer-foundation"` 確認實作前失敗。

## 2. Route、Subject Key 與 Progress

- [x] 2.1 實作 Add networkingV2 as an independent route and subject key 與 Networking v2 subject route：新增 `networkingV2` subject key、`/networking-v2` primary route/preload entry、`NetworkingV2View`，使直接開啟 route 顯示 `網路概論(v2)` 並使用 `subject-view-networking-v2`；以 `npm run typecheck` 與 `npx vitest run tests/unit/routeConfig.spec.ts tests/unit/routePreload.spec.ts tests/component/SubjectRoutesSmoke.spec.ts` 驗證。
- [x] 2.2 實作 Keep progress storage on the existing v1 localStorage shape 與 Networking v2 route has independent topic ownership：讓 progress empty state、invalid JSON normalization、舊 v1 state normalization 都補齊 `networkingV2`，且不搬移既有 `networking` topic ids；以 `npx vitest run tests/unit/subjectTopicProgressStorage.spec.ts tests/unit/networkingV2RouteWorkflow.spec.ts` 驗證。
- [x] 2.3 實作 Keep the entry inside the computer foundation menu 與 Computer foundation menu exposes Networking v2：在計概類 header menu 新增 `網路概論(v2)` 選項，選取後導向 `/networking-v2`，並維持 root redirect 與 top-level tabs 不變；以 `npx vitest run tests/component/ComputerFoundationSubjectSwitcher.spec.ts tests/component/AppShellSmoke.spec.ts` 與 `npx playwright test tests/e2e/app-shell.smoke.spec.ts -g "computer-foundation"` 驗證。

## 3. 網路概論(v2) 內容匯入

- [x] 3.1 實作 Convert Markdown into existing lessonArticle content blocks、Networking v2 topics preserve exact source traceability、Networking v2 topics keep source-authored teaching structure 的第 1 到第 4 篇：將 OSI 七層 + TCP/IP ★、基礎概念、資安：加密/雜湊/數位簽章/憑證與 TLS、網路設備對應層級（看得懂版）轉為 non-empty `lessonArticle` topics，保留來源段落、清單、表格與學習提示；以 `npx vitest run tests/unit/networkingV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts` 驗證 sourceFiles、titles、representative phrases 與 lessonArticle sections。
- [x] 3.2 實作 Convert Markdown into existing lessonArticle content blocks、Networking v2 topics preserve exact source traceability、Networking v2 topics keep source-authored teaching structure 的第 5 到第 8 篇：將無線與上網接取設備、IP 基礎 + 子網路計算 ★、傳輸層、應用層協定 + Port Number 對照表 ★ 轉為 non-empty `lessonArticle` topics，保留計算流程、協定對照、port 表格與來源順序；以 `npx vitest run tests/unit/networkingV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts` 驗證 sourceFiles、titles、representative phrases 與 lessonArticle sections。
- [x] 3.3 實作 Convert Markdown into existing lessonArticle content blocks、Networking v2 route keeps source-authored structure、Networking v2 topics keep source-authored teaching structure 的第 9 到第 12 篇：將實體層 + 標準速度表 ★、資料鏈結層、資安觀念與加密 ★、防禦設備與攻擊類型 ★ 轉為 non-empty `lessonArticle` topics，保留速度/標準表、資料鏈結層整理、資安攻防分類與 review summaries；以 `npx vitest run tests/unit/networkingV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts` 驗證 sourceFiles、titles、representative phrases 與 lessonArticle sections。
- [x] 3.4 完成 Networking v2 route uses filename-backed topics、Use source filenames as the order manifest and sanitized topic titles、Networking v2 topics preserve exact source traceability 的全量資料契約：確認 12 個 `sourceBatch: 'networking-v2-route'`、`sourceSummary`、lessonArticle `sourceSection`、`difficulty`、`topicType`、`terms`、`examOutline`、`memoryPoints`、`understandingNotes` 都完整，且 titles 不含 `網路概論_` 或前導章節 marker；以 `npx vitest run tests/unit/networkingV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts tests/unit/subjectTopics.spec.ts tests/unit/staleProfessionalContentAudit.spec.ts` 驗證。
- [x] 3.5 執行專業科目內容審查：逐篇比對 12 篇來源 MD 與 `networkingV2` formal topics，確認定義正確、協定/計算邏輯完整、表格與例子未遺漏，並記錄本變更為 lecture-only 因此干擾選項區分、4 個選項、1 個正解與選項辨析不適用；以 `npx vitest run tests/unit/networkingV2RouteWorkflow.spec.ts` 的代表文字斷言與手動內容審查紀錄驗證。

## 4. 文件、Smoke 與總驗證

- [x] 4.1 同步 `PROJECT_ARCHITECTURE.md`：讓架構文件列出 `/networking-v2`、`networkingV2`、`src/modules/networkingV2/views/NetworkingV2View.vue`、`src/modules/subjectTopics/data/networkingV2Topics.ts` 與計概類 header option；以 `rg -n "networking-v2|networkingV2|NetworkingV2View|網路概論\(v2\)" PROJECT_ARCHITECTURE.md` 與 `npx vitest run tests/unit/projectArchitecture.spec.ts` 驗證。
- [x] 4.2 完成 route 與 UI smoke 驗證：確認 `/networking-v2` 直接載入第一個 title `OSI 七層 + TCP/IP ★` 與最後一個 title `防禦設備與攻擊類型 ★`，計概類 menu 可在手機與桌機寬度導向 v2，且離線情境下 bundled static content 仍可由 PWA shell 呈現；以 `npx playwright test tests/e2e/app-shell.smoke.spec.ts tests/e2e/app-shell-mobile.spec.ts tests/e2e/pwa-offline-shell.spec.ts` 驗證。
- [x] 4.3 完成整體驗證與 Spectra artifact 檢查：執行 `npm run typecheck`、`npx vitest run tests/unit/routeConfig.spec.ts tests/unit/routePreload.spec.ts tests/unit/subjectTopicProgressStorage.spec.ts tests/unit/networkingV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts tests/unit/subjectTopics.spec.ts tests/component/ComputerFoundationSubjectSwitcher.spec.ts tests/component/SubjectRoutesSmoke.spec.ts`、`spectra analyze networking-v2-route --json`、`spectra validate networking-v2-route`，確認 proposal、design、specs、tasks 與實作行為一致。
