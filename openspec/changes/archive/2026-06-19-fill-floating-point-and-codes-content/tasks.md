## 1. 測試先行

- [x] 1.1 Implement `Lesson article sections can be collapsed when configured` test coverage by adding `SubjectTopicPage` unit assertions that a collapsible lessonArticle section is closed by default, opens on heading activation, closes on second activation, and leaves non-collapsible sections expanded; verify with `npm run test:unit -- tests/unit/SubjectTopicPage.spec.ts` failing before implementation and passing after implementation.
- [x] [P] 1.2 Implement `Computer Principles floating-point and code topics contain finalized lesson articles` content expectations by adding `professionalTopics.spec.ts` assertions for both topic ids, titles, sourceFiles, terms, section headings, key strings, corrected `P4`, decimal-fraction wording, and absence of raw display instructions; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts` failing before content implementation and passing after implementation.
- [x] [P] 1.3 Implement `Computer Principles route includes filled floating-point and code topics` routing expectations by updating `subjectTopics.spec.ts`, `computerPrinciplesRouteWorkflow.spec.ts`, and `staleProfessionalContentAudit.spec.ts` so the two topic ids are route-visible after `cp-complement-conversion` and treated as filled Computer Principles content; verify the three targeted suites fail before content implementation and pass after implementation.

## 2. 型別與 renderer 行為

- [x] 2.1 Implement `Add section-level collapse metadata to LessonArticleSection` by extending `LessonArticleSection` with optional `collapsible?: boolean` and `defaultExpanded?: boolean`, and by updating `SubjectTopicPage.vue` so collapsible sections keep local non-persisted open state while non-collapsible sections render expanded; verify with `npm run test:unit -- tests/unit/SubjectTopicPage.spec.ts` and `npm run typecheck`.

## 3. Computer Principles 內容匯入

- [x] 3.1 Implement `Keep imported content in professionalTopics static data` for `cp-floating-point-conversion` so the route shows `浮點數轉換(Floating-Point Conversion)` with IEEE 754 fields, encoding flow, normalization, decimal fraction to binary, 10.25 single precision, decoding, 0.1 inexactness, common pitfalls, and exam quick review; verify `professionalTopics.spec.ts` finds required sourceFiles and key strings including `IEEE 754`, `bias`, `0x41240000`, and the decimal-fraction step that removes the integer part before continuing.
- [x] 3.2 Implement `Keep imported content in professionalTopics static data` for `cp-codes-and-check-codes` so the route shows `數碼、文字碼與檢查碼(Codes and Check Codes)` with overview, BCD, Gray Code, character codes, Parity Check, CRC, Hamming Code, Hamming Distance, and a collapsible `考前總複習(Exam Quick Review)` section; verify `professionalTopics.spec.ts` finds `BCD`, `Gray Code`, `Unicode`, `UTF-8`, `CRC`, `Hamming Distance`, `Syndrome`, and collapse metadata on the review section.
- [x] 3.3 Implement `Normalize source instructions while preserving learner content` across both imported topics so raw directives `用table`, `用table做`, `用UL/LI表示`, and `此處用 UL/LI表示` are not serialized, Hamming(7,4) uses `P1`, `P2`, `P4`, and BCD `0000` to `1001` is scoped to one 8421 BCD digit; verify with `professionalTopics.spec.ts` content assertions.
- [x] 3.4 Implement `Preserve existing route order after complement conversion` by ensuring the visible Computer Principles topic id sequence contains `cp-complement-conversion`, `cp-floating-point-conversion`, and `cp-codes-and-check-codes` consecutively while other empty skeletons remain hidden; verify with `npm run test:unit -- tests/unit/subjectTopics.spec.ts`.
- [x] 3.5 Update manual review traceability so `_private/TMP/computer-principles/manual-review.md` records `cp-floating-point-conversion` and `cp-codes-and-check-codes` as reviewed filled content with their Markdown sources and known corrections; verify by content review that the file names both topic ids, both source Markdown paths, and the `P4` correction.

## 4. 最終驗證

- [x] 4.1 Run `npm run test:unit -- tests/unit/SubjectTopicPage.spec.ts tests/unit/professionalTopics.spec.ts tests/unit/subjectTopics.spec.ts tests/unit/computerPrinciplesRouteWorkflow.spec.ts tests/unit/staleProfessionalContentAudit.spec.ts`, `npm run typecheck`, and `npm run build`; completion requires all commands to pass without new console errors.
- [x] 4.2 Perform a mobile-width manual review of `/computer-principles` with `cp-floating-point-conversion` and `cp-codes-and-check-codes` expanded, verify the two topics appear after `cp-complement-conversion`, `考前總複習(Exam Quick Review)` is closed by default and opens on tap, table/orderedList content remains readable, and an offline reload still renders the static topic data.

## 5. 十七章內容貼近原 Markdown

- [x] 5.1 Implement `Computer Principles floating-point and code topics contain finalized lesson articles` regression coverage by extending `professionalTopics.spec.ts` so `cp-codes-and-check-codes` must preserve the source Markdown learning order and details, including `數碼管數字，文字碼管文字，檢查碼管有沒有錯。`, 259 的 8421 BCD example, Binary `1011` to Gray `1110`, Gray `1110` to Binary `1011`, CRC modulo-2 steps, Hamming `2^r ≥ m + r + 1`, Syndrome `S4 S2 S1`, Hamming Distance formulas, and absence of raw display instructions; verify the test fails before content rewrite and passes after implementation.
- [x] 5.2 Implement `Keep codes and check-codes structure close to the source Markdown` by rewriting `cp-codes-and-check-codes` lessonArticle sections in `professionalTopics.ts` to follow the original Markdown sequence while only cleaning layout and correcting known errors such as `P1、P2、P4`; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts`.
- [x] 5.3 Re-run final verification for the adjusted content with `npm run test:unit -- tests/unit/professionalTopics.spec.ts tests/unit/SubjectTopicPage.spec.ts tests/unit/subjectTopics.spec.ts`, `npm run typecheck`, and `npm run build`; completion requires all commands to pass.
- [x] 5.4 Implement `Keep codes and check-codes structure close to the source Markdown` subsection rendering so `Gray Code` contains `Gray Code 解釋與用途`, `Binary 轉 Gray`, and `Gray 轉 Binary` as section-internal subsections with rules and examples, not top-level lesson sections; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts tests/unit/SubjectTopicPage.spec.ts` and `npm run typecheck`.
- [x] 5.5 Implement `Keep codes and check-codes structure close to the source Markdown` CRC rendering so `CRC` contains `一、定義與用途`, `二、傳送與接收流程`, and `三、算法` as section-internal subsections with the original seven modulo-2 algorithm steps; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts` and `npm run typecheck`.
- [x] 5.6 Implement `Keep codes and check-codes structure close to the source Markdown` Hamming rendering so `Hamming Code（漢明碼）` contains the source-style subsections for basic concept, `3-1`, `3-2`, `3-3`, `3-4`, and `Syndrome（症候值/症狀碼/校驗子）`, while `Syndrome` is not a top-level lesson section; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts` and `npm run typecheck`.
- [x] 5.7 Refine `Hamming Code（漢明碼）` rendering so it no longer forces every learning step into `subsection` headings, and instead follows the source Markdown note flow with basic list items, `3` / `3-1` / `3-2` / `3-3` / `3-4` paragraphs, a Hamming(7,4) position table, and Syndrome kept inside the Hamming Code block; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts`, `npm run typecheck`, and `npm run build`.
- [x] 5.8 Refine `Hamming Code（漢明碼）` note layout so `3-1`, `3-2`, `3-3`, and `3-4` visually indent under `3. 編碼步驟`, with nested formula/explanation lines indented one level deeper; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts`, `npm run typecheck`, and `npm run build`.
- [x] 5.9 Refine `Hamming Code（漢明碼）` note layout so main steps `1`, `2`, and `3` align in the same ordered list, while `3-1`, `3-2`, `3-3`, and `3-4` remain as shallow inner notes under the third step; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts`, `npm run typecheck`, and `npm run build`.
- [x] 5.10 Implement `indentedGroup` lessonArticle rendering so Hamming Code `3-1`, `3-2`, Hamming(7,4), its table, the data-placement note, `3-3`, and `3-4` share the same visual left boundary under the aligned `1` / `2` / `3` ordered list; verify with targeted unit tests, `npm run typecheck`, `npm run build`, and Playwright screenshot review of `/computer-principles`.
- [x] 5.11 Refine nested `indentedGroup` rendering so Hamming Code P1/P2/P4 check-bit details and the final “此步驟可以得出全部漢明碼” line sit one visual indentation level under `3-3. 算每個校驗位`; verify with targeted unit tests, `npm run typecheck`, `npm run build`, and Playwright screenshot review of `/computer-principles`.
- [x] 5.12 Refine `考前總複習(Exam Quick Review)` so it follows the Markdown categories by grouping content into `常見陷阱`, `國考答題句`, and `考前速記` subsections inside the collapsible review section; verify with `npm run test:unit -- tests/unit/professionalTopics.spec.ts`, `npm run typecheck`, and `npm run build`.
- [x] 5.13 Refine collapsible lesson section spacing so `考前總複習(Exam Quick Review)` has visible top separation from the preceding section instead of appearing directly attached; verify with `npm run test:unit -- tests/unit/SubjectTopicPage.spec.ts`, `npm run typecheck`, and `npm run build`.
