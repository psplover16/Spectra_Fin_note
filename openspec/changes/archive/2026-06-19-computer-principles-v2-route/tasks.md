## 1. 測試基線

- [x] [P] 1.1 建立 Add computerPrinciplesV2 as an independent route and subject key、Keep progress storage on the existing v1 localStorage shape、Computer Principles v2 subject route、Computer Principles v2 route has independent topic ownership 的失敗測試：驗證 `/computer-principles-v2` 被列入 route config/preload、`subjectKeys` 與 progress empty state 含 `computerPrinciplesV2`、未知路由仍導回 `/computer-principles`、v2 topic ids 不出現在 `computerPrinciples`/`networking`/`digitalLogic`/`operatingSystems`；以 `npx vitest run tests/unit/routeConfig.spec.ts tests/unit/routePreload.spec.ts tests/unit/subjectTopicProgressStorage.spec.ts tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts` 確認實作前失敗。
- [x] [P] 1.2 建立 Use 00 catalog as a title manifest but not as learner content、Convert Markdown into existing lessonArticle content blocks、Computer principles v2 route uses catalog-backed topics、Computer principles v2 titles follow the catalog manifest、Computer principles v2 uses lessonArticle content、Computer Principles v2 topics preserve source traceability、Computer Principles v2 topics keep source-authored teaching structure 的失敗測試：驗證 13 個 source files、13 個 catalog titles、`00_目錄.md` 不成為 route-visible topic、每個 topic 有非空 lessonArticle、sourceSummary/sourceSection 可追溯、無題庫欄位需求；以 `npx vitest run tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts tests/unit/subjectTopics.spec.ts` 確認實作前失敗。

## 2. Route、Subject Key 與 Progress

- [x] 2.1 實作 Add computerPrinciplesV2 as an independent route and subject key 與 Computer Principles v2 subject route：新增 `computerPrinciplesV2` subject key、`/computer-principles-v2` primary route/preload entry、`ComputerPrinciplesV2View`，使直接開啟 route 顯示 `計概(v2)` 並使用獨立 progress namespace；以 `npm run typecheck` 與 `npx vitest run tests/unit/routeConfig.spec.ts tests/unit/routePreload.spec.ts tests/component/SubjectRoutesSmoke.spec.ts` 驗證。
- [x] 2.2 實作 Keep progress storage on the existing v1 localStorage shape：讓 `createEmptySubjectTopicProgressState()`、invalid JSON normalization、舊 v1 state normalization 都補齊 `computerPrinciplesV2` 且不搬移既有 `computerPrinciples` topic ids；以 `npx vitest run tests/unit/subjectTopicProgressStorage.spec.ts` 驗證。
- [x] 2.3 實作 Computer foundation menu exposes Computer Principles v2：在計概類 header menu 新增 `計概(v2)` 選項，選取後導向 `/computer-principles-v2`，root redirect 保持 `/computer-principles`；以 `npx vitest run tests/component/ComputerFoundationSubjectSwitcher.spec.ts tests/component/AppShellSmoke.spec.ts` 與 `npx playwright test tests/e2e/app-shell.smoke.spec.ts -g "computer-foundation"` 驗證。

## 3. 計概(v2) 內容匯入

- [x] 3.1 實作 Use 00 catalog as a title manifest but not as learner content 與 Computer principles v2 titles follow the catalog manifest：建立 v2 topic order/title 對照，使 13 個 route-visible titles 完全採 `00_目錄.md` 篇名欄，且不顯示 `00_`、`01_` 或 `基本計概 01：` 前綴；以 `npx vitest run tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/subjectTopics.spec.ts` 驗證 title/order 與 `00_目錄.md` 排除。
- [x] 3.2 實作 Convert Markdown into existing lessonArticle content blocks 與 Computer principles v2 uses lessonArticle content 的 01 到 05 篇：將架構與計算理論、機器指令與指令週期、Pipeline 與 Hazard、效能與 RISC／CISC、匯流排與 USB 轉為 non-empty lessonArticle topics，保留來源段落、清單、表格與計算步驟；以 `npx vitest run tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts` 驗證前 5 篇 sourceFiles、representative phrases 與 lessonArticle sections。
- [x] 3.3 實作 Convert Markdown into existing lessonArticle content blocks 與 Computer Principles v2 topics keep source-authored teaching structure 的 06 到 10 篇：將記憶體階層與分類、暫存器與 Cache、進制轉換、補數轉換、浮點數轉換轉為 non-empty lessonArticle topics，保留計算題與練習說明而不改寫為生成摘要；以 `npx vitest run tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts` 驗證中段 5 篇內容結構與代表性計算文字。
- [x] 3.4 實作 Convert Markdown into existing lessonArticle content blocks、Computer Principles v2 topics preserve source traceability、Computer Principles v2 topics keep source-authored teaching structure 的 11 到 13 篇與全量資料契約：將數碼與文字碼、Parity 與 CRC、漢明碼與漢明距轉為 non-empty lessonArticle topics，確認 13 篇 `sourceBatch`、sourceFiles、sourceSummary/sourceSection、topicType/difficulty/terms 合理，且不新增 multiple-choice question fields、answer uniqueness metadata 或 option distinguishability metadata；以 `npx vitest run tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts tests/unit/staleProfessionalContentAudit.spec.ts` 驗證。

## 4. 文件、Smoke 與總驗證

- [x] 4.1 同步 `PROJECT_ARCHITECTURE.md`，讓架構文件列出 `/computer-principles-v2`、`computerPrinciplesV2`、`src/modules/computerPrinciplesV2/views/ComputerPrinciplesV2View.vue` 與計概類 header option；以 `rg -n "computer-principles-v2|computerPrinciplesV2|ComputerPrinciplesV2View|計概\(v2\)" PROJECT_ARCHITECTURE.md` 與 `npx vitest run tests/unit/projectArchitecture.spec.ts` 驗證。
- [x] 4.2 完成 route 與 UI smoke 驗證：確認 `/computer-principles-v2` 直接載入第一個 title `架構與計算理論` 與最後一個 title `檢查碼（二）漢明碼與漢明距`，計概類 menu 可從手機與桌機寬度導向 v2，且離線情境下 bundled static content 仍可由 PWA shell 呈現；以 `npx playwright test tests/e2e/app-shell.smoke.spec.ts tests/e2e/app-shell-mobile.spec.ts tests/e2e/pwa-offline-shell.spec.ts` 驗證。
- [x] 4.3 完成整體驗證與 Spectra artifact 檢查：執行 `npm run typecheck`、`npx vitest run tests/unit/routeConfig.spec.ts tests/unit/routePreload.spec.ts tests/unit/subjectTopicProgressStorage.spec.ts tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts tests/unit/subjectTopics.spec.ts tests/component/ComputerFoundationSubjectSwitcher.spec.ts tests/component/SubjectRoutesSmoke.spec.ts`、`spectra analyze computer-principles-v2-route --json`、`spectra validate computer-principles-v2-route`，確認 proposal、design、specs、tasks 與實作行為一致。
