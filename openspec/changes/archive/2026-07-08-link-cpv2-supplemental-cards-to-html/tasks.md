## 1. RED 測試契約

- [x] [P] 1.1 為 `Computer principles v2 route uses catalog-backed topics`、`Floating point conversion topic uses refreshed v2 source`、`Floating point topic exposes practice and IEEE 754 special-value sections` 補 RED unit tests：`computerPrinciplesV2` 必須有 21 張、leading order 為 9 張指定順序、`cpv2-floating-point-conversion` 位於第 18 張且仍是 catalog 第 10 張；以 `npx vitest run tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/subjectTopics.spec.ts` 驗證目前因舊 20 張契約失敗。
- [x] [P] 1.2 為 `Computer Principles v2 topics preserve source traceability` 與 `Computer Principles v2 split supplemental topics trace sources` 補 RED source traceability tests：6 張 HTML-linked topics 必須有新 Markdown `sourceFiles`、`htmlPage.sourceFilename`，且不得再有 `cpv2-supplemental-basic-data-structures`；以 `npx vitest run tests/unit/professionalTopics.spec.ts tests/unit/subjectTopics.spec.ts` 驗證目前因舊來源與舊 id 失敗。
- [x] [P] 1.3 為 `Computer principles v2 publishes supplemental HTML lessons`、`Computer principles v2 HTML lessons preserve Markdown layout`、`Computer principles v2 HTML lessons use database v2 reading chrome` 補 RED static asset tests，並覆蓋設計決策「使用 standalone HTML 靜態資產而非 runtime Markdown renderer」與「HTML 轉換保留 Markdown 原始排版」：檢查 6 個 HTML 檔、`zh-Hant`、左上返回、無右側 recite toggle、`<ol>`/`<ul>`/table/code/blockquotes marker；以 `npx vitest run tests/unit/computerPrinciplesV2HtmlPages.spec.ts` 驗證目前因 HTML 尚未產生失敗。
- [x] [P] 1.4 為設計決策「SubjectTopicCard 依 htmlPage 切換 title control 行為」與 `Computer principles v2 uses lessonArticle content` 補 RED component smoke：HTML-linked card title 必須是 anchor 且不展開 detail，`阿姆達爾定律` 仍用 button 展開 inline lessonArticle；以 `npx vitest run tests/component/SubjectRoutesSmoke.spec.ts` 驗證目前因 title 仍是 button 或資料未更新失敗。
- [x] [P] 1.5 為 `Computer principles v2 HTML lessons are packaged for offline use` 補 RED production PWA smoke：online warmup 開啟 `/computer-principles-v2` 與一個 CPv2 HTML lesson，離線後 route shell 與 warmed HTML asset 仍可讀；以 `npm run build` 後 `PLAYWRIGHT_USE_PREVIEW=1 npx playwright test tests/e2e/pwa-offline-shell.spec.ts` 驗證目前因 HTML link/asset 尚未存在失敗。

## 2. 實作 HTML 與 topic 連結

- [x] 2.1 實作設計決策「在 SubjectTopic 加入 optional htmlPage 連結資料」：新增 `SubjectTopicHtmlPage` 與 `SubjectTopic.htmlPage?: SubjectTopicHtmlPage`，新增 `createComputerPrinciplesV2PageHref()` 並讓 href 尊重 `import.meta.env.BASE_URL`；以 `npx vitest run tests/unit/computerPrinciplesV2HtmlPages.spec.ts tests/unit/subjectTopics.spec.ts` 驗證 href base-path cases 與型別契約。
- [x] 2.2 依 `Computer principles v2 publishes supplemental HTML lessons` 產生 `public/computer-principles-v2/` 六個 standalone HTML，保留來源 Markdown、使用 database-v2 紙張風格與 base-path-aware 返回按鈕，且不加入右側 recite toggle；以 `npx vitest run tests/unit/computerPrinciplesV2HtmlPages.spec.ts` 驗證 inventory、reading chrome 與 Markdown layout markers。
- [x] 2.3 更新 CPv2 professional topic data 以滿足 `Computer principles v2 route uses catalog-backed topics` 與 `Floating point topic exposes practice and IEEE 754 special-value sections`：移除 `cpv2-supplemental-basic-data-structures`，新增 `cpv2-supplemental-complexity-linear-structures` 與 `cpv2-supplemental-trees-hash-tables`，六張指定卡加入 `htmlPage`，`阿姆達爾定律` 維持 inline lessonArticle；以 `npx vitest run tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts tests/unit/subjectTopics.spec.ts` 驗證 21 張順序、來源與 removed id。
- [x] 2.4 實作設計決策「保留 localStorage 進度命名空間且不 migration」：`getSubjectTopics()` 將有 `htmlPage.href` 的 topic 視為 route-visible content，既有 `computerPrinciplesV2` localStorage key 與 state shape 不變，舊 removed id 不渲染且不新增 migration；以 `npx vitest run tests/unit/subjectTopics.spec.ts tests/unit/subjectTopicProgressStorage.spec.ts` 與 `rg "cpv2-supplemental-basic-data-structures|migration" src/modules/subjectTopics` 驗證。
- [x] 2.5 實作 `SubjectTopicCard` / `SubjectTopicPage` 的 HTML-linked title 行為以滿足 `Computer principles v2 uses lessonArticle content`：有 `htmlPage` 時 title control 為 `<a href>` 且不展開 detail，無 `htmlPage` 時維持 button 展開；以 `npx vitest run tests/component/SubjectRoutesSmoke.spec.ts tests/unit/SubjectTopicPage.spec.ts` 驗證。
- [x] 2.6 完成專業科目內容審查：確認 6 份 HTML 的來源、標題、重點內容、lecture-only 邊界與「不擴寫近 8 年考古題」範圍一致，並在 `_TMP/reviews/cpv2-html-linked-supplemental-content-review.md` 記錄審查；以 `rg "CPU排班演算法_國考完整講義|lecture-only|6 個 HTML" _TMP/reviews/cpv2-html-linked-supplemental-content-review.md` 驗證。

## 3. 收斂驗證

- [x] 3.1 執行 typecheck 與 targeted unit/component tests，證明 route order、HTML inventory、source traceability、`SubjectTopicCard` htmlPage 行為與 `Floating point conversion topic uses refreshed v2 source` 全部通過；驗證指令為 `npm run typecheck` 與 `npx vitest run tests/unit/computerPrinciplesV2HtmlPages.spec.ts tests/unit/computerPrinciplesV2RouteWorkflow.spec.ts tests/unit/professionalTopics.spec.ts tests/unit/subjectTopics.spec.ts tests/unit/SubjectTopicPage.spec.ts tests/component/SubjectRoutesSmoke.spec.ts`。
- [x] 3.2 執行 production build 與 PWA offline smoke，證明 `Computer principles v2 HTML lessons are packaged for offline use` 且 warmed CPv2 HTML asset 離線可讀；驗證指令為 `npm run build` 與 `PLAYWRIGHT_USE_PREVIEW=1 npx playwright test tests/e2e/pwa-offline-shell.spec.ts`。
- [x] 3.3 執行 Spectra 與 artifact 自查，證明 proposal/design/spec/tasks 一致、無 placeholder、無 spec drift，且本次實作檔案 whitespace 乾淨；驗證指令為 `spectra analyze link-cpv2-supplemental-cards-to-html --json`、`spectra validate link-cpv2-supplemental-cards-to-html`、`git diff --check -- openspec/changes/link-cpv2-supplemental-cards-to-html src public tests _TMP/reviews`。
